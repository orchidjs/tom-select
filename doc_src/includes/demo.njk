{% set demo_count = [] %}

{% macro demo(label,html,script,style) %}
	{% set dd = demo_count.push('a') %}

	<div class="demo border-bottom py-5">

		{{ label | safe }}
	<div class="row">
		<div class="col-lg-6 mb-3">
			<div class="demo-html">
				{{ html | safe }}
			</div>
			<script>{{ script | safe }}</script>
			<style>{{ style | safe }}</style>
		</div>
		<div class="col-lg-6">

			<ul class="nav nav-tabs" role="tablist">
			  <li class="nav-item" role="presentation">
			    <a class="nav-link active" data-toggle="tab" data-target="#javascript-tab-{{ demo_count | length }}" role="tab" aria-controls="home" aria-selected="true">JavaScript</a>
			  </li>
			  <li class="nav-item" role="presentation">
			    <a class="nav-link" data-toggle="tab" data-target="#html-tab-{{ demo_count | length }}" role="tab" aria-controls="profile" aria-selected="false">HTML</a>
			  </li>
			  {% if style %}
			  <li class="nav-item" role="presentation">
			    <a class="nav-link" data-toggle="tab" data-target="#style-tab-{{ demo_count | length }}" role="tab" aria-controls="profile" aria-selected="false">CSS</a>
			  </li>
			  {% endif %}
			</ul>

			<div class="tab-content">
			  <div class="tab-pane fade show active" id="javascript-tab-{{ demo_count | length }}" role="tabpanel" aria-labelledby="home-tab">
				  <textarea class="form-control" readonly rows="7">{{ script }}
				  </textarea>
			  </div>
			  <div class="tab-pane fade" id="html-tab-{{ demo_count | length }}" role="tabpanel" aria-labelledby="profile-tab">
				  <textarea class="form-control" readonly rows="7">{{ html }}
				  </textarea>
			  </div>

			  {% if style %}
			  <div class="tab-pane fade" id="style-tab-{{ demo_count | length }}" role="tabpanel" aria-labelledby="profile-tab">
				  <textarea class="form-control" readonly rows="7">{{ style }}
				  </textarea>
			  </div>
			  {% endif %}

			</div>
		</div>
	</div>
	</div>
{% endmacro %}
